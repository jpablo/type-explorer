package org.jpablo.typeexplorer.backend.webApp

import zio.test.*
import zio.test.Assertion.*

class ModelsSpec extends ZIOSpecDefault {

  override def spec = suite("models spec") {
    test("a") {
      assertTrue(1 == 2)
    }

  }
  
}
